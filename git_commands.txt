the tags that i learned are:

git add <file> - used for staging the file
git commit -m "message about the commit" - used to commit the file
git push origin main - used to push the file to github
git init - used to create a git repositary in local machine
git tag -a "tag name" - used to create  a version "-a" means annotation
git log - used to see the changes
git tag - it show the tags
git push origin tag_name - used to push the tag to github
git clone <repositary url>

git checkout -b <branch name>
    or
git switch -b <branch name>
